<nav class=" font-sans flex flex-col text-center sm:flex-row sm:text-left sm:justify-between py-4 px-6 bg-white shadow sm:items-baseline w-full"> 
  <div class="mb-2 sm:mb-0">
    <a href="/" class="text-lg no-underline text-grey-darkest hover:text-blue-dark">Chatroom</a>
    <a href="#" class="text-lg no-underline text-grey-darkest hover:text-blue-dark">Messages</a>
  </div>
      
  <!-- Dropdown Menu -->
  <div data-controller="dropdown">
    <button data-action="click->dropdown#toggle" class=" text-black px-4 py-2 rounded">
      Account
    </button>
    <div data-dropdown-target="menu" class="hidden absolute bg-white shadow-md rounded mt-2">
      <% if logged_in? %>
        <%= link_to "Log out", logout_path, method: :delete, class: "block px-4 py-2 text-gray-800 hover:bg-gray-200"%>
      <% else %>
        <%= link_to "Log in", login_path, class: "block px-4 py-2 text-gray-800 hover:bg-gray-200" %>
      <% end%>
    </div>
    <% if !logged_in? %>
      <%= link_to "Log Up", login_path, class: "rounded bg-blue-400 text-white p-2" %>
      
    <% end %>
  </div>
</nav>